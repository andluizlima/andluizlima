nano - Abre o arquivo para edição
mkdir -p - Cria uma nova pasta

GIT

git init
git status
git add nomearquivo
git add -A
git commit -m "MENSAGEM PARA DAR O COMMIT SENÃO DÁ XIBÚ"
git push -u origin master
